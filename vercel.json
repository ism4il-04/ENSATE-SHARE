{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/[...path]"
        }
    ],
    "functions": {
        "api/**/*.ts": {
            "maxDuration": 30
        }
    }
}